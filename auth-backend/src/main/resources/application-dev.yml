#development version of configurations
server:
  port: 8083

# database configurations
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_app
    username: root
    password: password

#    hikari:
#      pool-name: HikariCP
#      maximum-pool-size: 5
#      connection-timeout: 10000
#      idle-timeout: 600000
#      max-lifetime: 1800000
#      validation-timeout: 1800000
#      leak-detection-threshold: 60000
#      initialization-fail-timeout: -1

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true

logging:
  level:
    org.springframework.security: DEBUG

security:
  jwt:
    secret: ${JWT_SECRET:47af7c3f89cc24fc4de30a9daafd8b88daa6ceace431fdd2de8e9e3944b1831c}
    issuer: ${JWT_ISSUER:api.swarnavo.in}
    access_ttl_seconds: ${JWT_ACCESS_TTL_SECONDS:3600}
    refresh_ttl_seconds: ${JWT_REFRESH_TTL_SECONDS:86400}
    refresh_token_cookie_name: ${JWT_REFRESH_COOKIE_NAME:refreshToken}
    cookie-secure: ${JWT_COOKIE_SECURE:true}
    cookie-http-only: ${JWT_COOKIE_HTTP_ONLY:true}
    cookie-same-site: ${JWT_COOKIE_SAME_SITE:lax}
    cookie-domain: ${JWT_COOKIE_DOMAIN:localhost}